<Page
    x:Class="Mindmappy.Shared.UIEdge"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mindmappy.Shared"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding RelativeSource={RelativeSource Self}}"
    mc:Ignorable="d"
    Canvas.Top="0"
    Canvas.Left="0"
    Canvas.ZIndex="1">
    <Grid>
        <Path x:Name="background" Data="{Binding Path=PathData}" Stroke="Transparent" StrokeThickness="10" />
        <Path x:Name="foreground" Data="{Binding Path=PathData}" Stroke="{Binding Path=StrokeColor}" StrokeThickness="{Binding Path=StrokeThickness}" />
    </Grid>
</Page>
