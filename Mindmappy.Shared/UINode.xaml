<Page
    x:Class="Mindmappy.Shared.UINode"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mindmappy.Shared"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding RelativeSource={RelativeSource Self}}"
    mc:Ignorable="d"
    Canvas.Left="{Binding Left}"
    Canvas.Top="{Binding Top}"
    Canvas.ZIndex="2">
    <Grid Width="{Binding Path=RectWidth}" Height="{Binding Path=RectHeight}">
        <Rectangle x:Name="rect" Fill="LightGray" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" />
        <TextBox
            Background="Transparent"
            BorderThickness="0"
            VerticalAlignment="Center" 
            HorizontalAlignment="Center"
            AcceptsReturn="True"
            Padding="5"
            Margin="10"
            x:Name="textBox"
            Text="{Binding Path=Label, Mode=TwoWay}" />
    </Grid>
</Page>
